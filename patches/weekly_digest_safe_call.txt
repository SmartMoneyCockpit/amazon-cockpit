
from utils.safe import try_or_log

ok, res = try_or_log(generate, "Weekly Digest: generate()")
if not ok:
    st.error(res)
else:
    st.success(f"Generated for week {res.get('week')}")
    st.session_state["_weekly_paths"] = res
