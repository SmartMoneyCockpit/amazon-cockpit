
# Simple healthcheck endpoint replacement for Render -- use as a script
# You can configure the Render Health Check to run this command:
#   python healthcheck.py
# It will exit(0) if OK.
import sys
sys.exit(0)
