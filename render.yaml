services:
  - type: web
    name: amazon-cockpit-2
    env: python
    plan: starter
    region: oregon
    buildCommand: pip install -c constraints.txt -r requirements.txt
    startCommand: ./start.sh
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
